# A. 체크리스트와 템플릿

이 부록은 책에서 반복해서 사용할 실무 체크리스트와 템플릿을 모은다. 그대로 복사해 작업 노트, 이슈, PR, 에이전트 지시문, 코드 리뷰 양식에 사용할 수 있다.

## AI 작업 요청 템플릿

```text
목표:
- 사용자 또는 제품 관점에서 무엇이 가능해져야 하는가?

변경 범위:
- 수정해도 되는 화면, 컴포넌트, 파일, API는 무엇인가?

기존 패턴:
- 따라야 할 컴포넌트, 훅, 타입, 테스트 스타일은 무엇인가?

상태 규칙:
- 지역 상태, 서버 상태, URL 상태, 파생 상태는 어떻게 나뉘는가?

완료 조건:
- 어떤 동작, 테스트, 문서가 확인되어야 끝났다고 볼 수 있는가?

검증 방법:
- 실행해야 할 타입 검사, 테스트, 브라우저 확인은 무엇인가?

제약:
- 건드리지 말아야 할 파일, 추가하면 안 되는 의존성, 바꾸면 안 되는 API는 무엇인가?

결과 보고:
- 변경, 검증, 미검증, 남은 위험을 구분해서 보고한다.
```

## 코드 읽기 노트 템플릿

```text
화면/기능:

사용자 흐름:

진입점:

라우트/레이아웃:

데이터 출처:

상태 위치:
- 지역 상태:
- 서버 상태:
- URL 상태:
- 공유 UI 상태:
- 파생 상태:

주요 타입:

핵심 컴포넌트:

외부 의존성:

테스트 위치:

최근 변경/커밋:

변경 위험:

남은 질문:
```

## 자동생성 코드 리뷰 체크리스트

- 요구사항을 실제로 만족하는가?
- 기존 코드 스타일과 패턴을 따르는가?
- 불필요한 새 의존성을 추가하지 않았는가?
- 서버 상태를 클라이언트 state로 불필요하게 복사하지 않았는가?
- URL에 남아야 할 상태를 지역 state로만 두지 않았는가?
- 파생 상태를 별도 state로 저장하지 않았는가?
- 로딩, 에러, 빈 상태, 취소 상태를 구분하는가?
- 오래된 응답, 중복 제출, optimistic update 실패를 다루는가?
- 접근성과 키보드 조작을 해치지 않는가?
- 민감 정보나 secret이 클라이언트에 노출되지 않는가?
- 테스트가 성공 케이스와 실패 케이스를 포함하는가?
- 사람이 설명할 수 있는 구조인가?

## 상태 분류 체크리스트

```text
값:

사용자가 직접 바꾸는가?

서버에서 온 값인가?

URL에 남아야 하는가?

여러 컴포넌트가 함께 읽거나 쓰는가?

기존 값으로 계산 가능한가?

시간이 지나면 stale해지는가?

저장 위치:
- 지역 상태
- 서버 상태
- URL 상태
- 공유 UI 상태
- 저장하지 않음
```

## 테스트 요청 템플릿

```text
대상 기능:

성공 케이스:

실패 케이스:

경계값:

비동기 위험:
- 오래된 응답
- 중복 클릭
- 취소
- 느린 네트워크

접근성 확인:

테스트 도구:
- 단위 테스트
- 컴포넌트 테스트
- E2E 테스트

기존 테스트 스타일:
```

## PR 검증 기록 템플릿

```text
변경:
- 

이유:
- 

검증:
- 타입 검사:
- 린트:
- 단위/컴포넌트 테스트:
- E2E:
- 브라우저 수동 확인:
- 접근성:
- 성능:

미검증:
- 

남은 위험:
- 
```

## 에이전트 결과 리뷰 템플릿

```text
에이전트 요약과 실제 diff가 일치하는가?

의도하지 않은 파일이 수정되었는가?

새 의존성이나 설정 변경이 있는가?

기존 패턴을 우회한 코드가 있는가?

검증 명령이 실제로 실행되었는가?

실패하거나 생략한 검증이 보고되었는가?

리뷰어가 직접 확인해야 할 부분은 무엇인가?
```

## AI 시대 일정 산정 템플릿

```text
작업 목표:

사용자에게 보이는 완료 상태:

AI로 단축 가능한 작업:
- 초안 생성:
- 반복 코드:
- 테스트 초안:
- 문서화:

사람이 판단해야 하는 작업:
- 요구사항:
- UX:
- 상태/아키텍처:
- 보안/개인정보:
- 리뷰/합의:

불확실성:
- 요구사항:
- 디자인:
- API:
- 기존 코드:
- 테스트/QA:

검증 범위:
- 타입/린트:
- 단위/컴포넌트 테스트:
- E2E:
- 접근성:
- 성능:
- 브라우저 수동 확인:

예상 일정:
- 탐색:
- 초안:
- 통합:
- 검증:
- 리뷰 반영:

범위 축소 옵션:

일정을 다시 산정할 시점:
```

## AI 일정 산정 워크숍 템플릿

15장의 일정 산정 논의를 팀 회의에서 사용할 때는 다음 30분 템플릿을 쓴다.

```text
회의 이름:

기능/작업:

참석자:
- PM/기획:
- 디자인:
- 프론트엔드:
- 백엔드:
- QA/운영:

0-5분. 완료 상태
- 사용자가 볼 수 있는 완료 상태:
- 이번 릴리즈에서 반드시 필요한 범위:
- 이번 릴리즈에서 제외 가능한 범위:

5-10분. AI로 빠르게 만들 수 있는 것
- UI 초안:
- 타입/계약 초안:
- 테스트 초안:
- 문서/체크리스트 초안:
- 의사결정용 프로토타입인지, 제품 코드 후보인지:

10-15분. 사람이 판단해야 하는 것
- UX/정책:
- 상태 위치:
- API 계약:
- 접근성:
- 성능:
- 보안/개인정보:
- 팀 합의:

15-20분. 검증 범위
- 타입/린트:
- 단위/컴포넌트 테스트:
- E2E/브라우저 확인:
- 접근성 확인:
- 성능 확인:
- QA 확인:

20-25분. 일정 범위
- 탐색:
- AI 초안:
- 통합:
- 검증:
- 리뷰 반영:
- 예상 범위:

25-30분. 재산정 조건
- 재산정 시점:
- 재산정에 필요한 정보:
- 담당자:
- 다음 회의/체크포인트:
```

회의 결과는 다음처럼 짧게 남긴다.

```text
결정:

AI 사용 범위:

남은 불확실성:

일정:

범위 축소 옵션:

재산정 시점:
```

## Part별 실습 과제 빠른 참조

각 Part를 읽은 뒤에는 다음 과제를 수행한다. 과제의 자세한 설명은 각 Part 마지막 장에 있다.

```text
Part 1. AI 이후의 프론트엔드
- 실제 작업 하나를 탐색, 초안, 검증, 보고 단계로 나눈다.
- 큰 지시문과 단계별 지시문을 비교한다.
- 검증한 항목과 검증하지 못한 항목을 기록한다.

Part 2. 변하지 않는 실행 환경
- DevTools로 Performance, Network, Application 기록을 확인한다.
- 브라우저 저장소와 API 요청의 경계를 설명한다.
- AI가 만든 리뷰 질문을 실제 브라우저 기록으로 보정한다.

Part 3. 복잡한 애플리케이션을 다루는 설계
- 기능 하나의 상태를 지역, 공유 UI, 서버, URL, 파생 상태로 분류한다.
- API 타입과 UI 타입을 분리한다.
- 가능한 UI 상태를 union type이나 상태 머신으로 표현한다.

Part 4. 코드를 읽고 통제하는 능력
- AI가 만든 나쁜 초안 diff를 리뷰한다.
- 요구사항, 패턴, 상태, 접근성, 성능, 보안 코멘트를 작성한다.
- 테스트 가능한 위험과 수동 확인이 필요한 위험을 분리한다.

Part 5. AI와 공생하는 개발자
- 기능 하나의 작업 헌장, 일정 산정, AI 지시문, 검증 기록을 하나로 묶는다.
- AI가 한 일과 사람이 판단한 일을 분리해 설명한다.
- 다음 12주 학습 계획에 반영할 기본기 하나를 정한다.
```

## Product Search Lab 실행

16장의 사례 연구를 코드로 확인하려면 다음 TypeScript lab을 실행한다.

```bash
cd frontend-ai-era/labs/product-search-state
../../../prompt-harness/labs/contract-harness/node_modules/.bin/tsc -p tsconfig.json
node --test test/*.test.mjs
```

브라우저 UI 흐름을 확인하려면 다음 파일을 연다.

```text
frontend-ai-era/labs/product-search-ui/index.html
```

lab의 테스트는 다음 계약을 검증한다.

- 빈 query parameter와 잘못된 값을 정규화한다.
- 기본값은 URL에서 생략한다.
- 필터 변경 시 페이지를 초기화한다.
- query key에 검색 조건과 사용자 맥락을 포함한다.
- API 응답을 UI 모델로 변환한다.
- 빈 상태와 에러 상태를 구분한다.
- optimistic update 실패 시 rollback할 수 있다.

## 출처

- GitHub Copilot docs: https://docs.github.com/en/copilot
- Testing Library docs: https://testing-library.com/docs/
- Playwright docs: https://playwright.dev/docs/intro
- OWASP Cheat Sheet Series: https://cheatsheetseries.owasp.org/
- Agile Alliance, Estimation: https://www.agilealliance.org/glossary/estimation/
- Local lab: `frontend-ai-era/labs/product-search-state`
- Local lab: `frontend-ai-era/labs/product-search-ui`
